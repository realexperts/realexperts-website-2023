---
import Button from './Button.astro';

type Props = {
  type: 'internal';
  title: string;
  url: string;
};

const { title, url } = Astro.props;
---

<Button color='red' size='lg' link={url} label={title} />
