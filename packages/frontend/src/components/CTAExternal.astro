---
import Button from './Button.astro';

type Properties = {
  type: 'external';
  title: string;
  url: string;
};

const { title, url } = Astro.props as Properties;
---

<Button
  color='red'
  size='lg'
  link={url}
  label={title}
  target='_blank'
  title={`${title} - öffnet in neuem Fenster`}
/>
