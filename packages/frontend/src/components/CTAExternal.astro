---
import Button from './Button.astro';

type Props = {
  type: 'external';
  title: string;
  url: string;
};

const { title, url } = Astro.props;
---

<Button
  color='red'
  size='lg'
  link={url}
  label={title}
  target='_blank'
  title={`${title} - öffnet in neuem Fenster`}
/>
