{
  "name": "@realexperts/backend",
  "version": "1.0.0",
  "author": "",
  "dependencies": {
    "directus": "^10.7.1",
    "directus-extension-models": "^2.5.2"
  },
  "description": "",
  "license": "ISC",
  "scripts": {
    "start": "directus bootstrap && directus start",
    "dev": "directus bootstrap && directus start",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
