{
  "name": "realexperts-website-2023",
  "version": "1.1.0",
  "description": "",
  "main": "index.js",
  "author": "",
  "license": "ISC",
  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "dev": "yarn workspaces foreach -Api run dev",
    "bootstrap": "yarn workspaces foreach -Api run bootstrap",
    "backend:start": "yarn workspace @realexperts/backend start",
    "backend:bootstrap": "yarn workspace @realexperts/backend bootstrap",
    "backend:start:bootstrap": "yarn workspace @realexperts/backend start:bootstrap",
    "export": "yarn run schema-sync:export @realexperts/backend && yarn run extension:models:export @realexperts/backend",
    "import": "yarn run schema-sync:import @realexperts/backend",
    "frontend:dev": "yarn workspace @realexperts/frontend dev",
    "frontend:start": "yarn workspace @realexperts/frontend start",
    "frontend:build": "yarn workspace @realexperts/frontend build",
    "lint": "yarn workspaces foreach -Api run lint",
    "lint:fix": "yarn workspaces foreach -Api run lint:fix",
    "check-types": "yarn workspaces foreach -Api run check-types"
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@4.9.2"
}
